Modal was introduced prior to Cisco UI Toolkit's introduction of it's own modal CSS rules.
Therefore the CSS classes used are unfortunately not those defined by CUI.

TBD: Generate on CUI modal CSS classes.
